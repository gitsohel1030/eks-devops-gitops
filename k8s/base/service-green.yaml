apiVersion: v1
kind: Service
metadata: 
  name: web-svc-green
  # namespace: prod-app
  labels:
    app: web-app
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
spec:
  type: ClusterIP
  selector:
    app: web-app
    version: green
  ports:
    - name: http
      port: 80
      targetPort: 80